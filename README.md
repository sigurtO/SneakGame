# SneakGame
An early demo using SO event channels to handle footsteps. The idea being the enemy AI never needs to know the player exists, it's just listening for the noise the player emits, meaning the two are completely decoupled.
